using System.Collections;
using System.Collections.Generic;
using Cam;
using UnityEngine;
using Door = Cam.Door;

public class ButtonDoorGlue : MonoBehaviour
{
	// public Button button;
	// public Door door;
		
	private void Awake()
	{
		// if (button != null)
		// {
			// button.turnOnEvent.AddListener(door.Open);
		// }
	}
}
